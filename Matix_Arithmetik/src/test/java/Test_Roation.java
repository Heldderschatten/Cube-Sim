import Calculation.RoomRotion;
import Objects.IntegerObjects.Point;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Test_Roation {
    /*
    @Test
    void Test_Roation() {
        Point points_X[][] =  new Point[3][2];
        points_X[0][0] = new Point(3,2,2);
        points_X[1][0] = new Point(2,2,3);
        points_X[2][0] = new Point(3,3,3);

        points_X[0][1] = new Point(3,-2,2);
        points_X[1][1] = new Point(2,-2,3);
        points_X[2][1] = new Point(2,-3,2);


        testPoint(points_X[0][1],RoomRotion.RotatePoint_X_90(points_X[0][0]));
        testPoint(points_X[1][1],RoomRotion.RotatePoint_X_90(points_X[1][0]));
        testPoint(points_X[2][1],RoomRotion.RotatePoint_X_90(points_X[2][0]));
    }

    void testPoint(Point p, Point soll) {
        assertEquals(soll.getX(), p.getX(),"X unterschieden");
        assertEquals(soll.getY(), p.getY(),"Y unterschieden");
        assertEquals(soll.getZ(),p.getZ(),"Z unterschieden");
    }
    */
}
